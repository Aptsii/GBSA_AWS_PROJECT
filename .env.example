# Local application mode. Copy this file to .env; never commit .env.
IEP_ENVIRONMENT=local
LOG_LEVEL=INFO
PUBLIC_BASE_URL=http://localhost:5173
APPLICANT_BASE_URL=http://localhost:5174
API_BASE_URL=http://localhost:8001

# Local PostgreSQL defaults. These values are for the disposable Compose service only.
POSTGRES_DB=interview_evidence
POSTGRES_USER=interview
POSTGRES_PASSWORD=interview_local
POSTGRES_PORT=5432
DATABASE_URL=postgresql+psycopg://interview:interview_local@localhost:5432/interview_evidence
IEP_DATABASE_URL=postgresql+psycopg://interview:interview_local@localhost:5432/interview_evidence

# Local AWS-compatible services. Application tests use deterministic fakes by default.
AWS_REGION=ap-northeast-2
IEP_AWS_REGION=ap-northeast-2
IEP_BEDROCK_REGION=ap-northeast-2
IEP_CROSS_REGION_MODEL_APPROVED=false
AWS_ENDPOINT_URL=http://localhost:4566
DYNAMODB_ENDPOINT_URL=http://localhost:8000
LOCALSTACK_PORT=4566
DYNAMODB_PORT=8000
OPENSEARCH_URL=http://localhost:9200
OPENSEARCH_PORT=9200
SUBMISSION_BUCKET=iep-local-submissions
IEP_OBJECT_STORAGE_BUCKET=iep-local-contract-fixtures
MEDIA_BUCKET=iep-local-media
ANALYSIS_QUEUE_NAME=iep-local-analysis

# Security-sensitive values must be supplied outside source control when a real adapter is enabled.
IEP_APPLICANT_SESSION_SECRET=
IEP_COMPANY_JWT_ISSUER=
IEP_COMPANY_JWT_AUDIENCE=
IEP_COMPANY_JWKS_URL=
IEP_APPLICANT_SESSION_TTL_SECONDS=3600
IEP_INVITATION_PUBLIC_BASE_URL=http://localhost:5174
IEP_INVITATION_EMAIL_TEMPLATE=applicant-invitation-v1
IEP_SIGNED_URL_TTL_SECONDS=300
IEP_DEFAULT_RETENTION_DAYS=180
IEP_EVENT_QUEUE_URL=
IEP_EVENT_DLQ_URL=
IEP_WORKER_MAX_ATTEMPTS=5
IEP_WORKER_WAIT_TIME_SECONDS=20
IEP_WORKER_VISIBILITY_TIMEOUT_SECONDS=60

# External AI calls remain disabled unless an approved profile and explicit opt-in are provided.
ENABLE_LIVE_AWS=false
AWS_PROFILE=
BEDROCK_MODEL_ID=
TRANSCRIBE_LANGUAGE_CODE=ko-KR
POLLY_VOICE_ID=Seoyeon
